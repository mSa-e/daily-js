# Coding Challenge #2

Use the BMI example from Challenge #1, and the code you already wrote, and improve it.

## Your tasks:

Print a nice output to the console, saying who has the higher BMI. The message is either "Mark's BMI is higher than John's!" or "John's BMI is higher than Mark's!"

Use a template literal to include the BMI values in the outputs. Example: "Mark's BMI (28.3) is higher than John's (23.9)!"

> Hint: Use an if/else statement 😃

GOOD LUCK 😃

> ## Note 1
>
> A Challenge can have more than one solution in different forms that depends on you **(No Issue in that)** but **it should ouputs the same output**

> ## Note 2
>
> This challange contains basic Javascript as it covers From `console.log` till `control statements` only and not above that
>
> In each new challenge will contain knew topics that builds on the old ones

> ## Note 3
>
> We will use Template literals
